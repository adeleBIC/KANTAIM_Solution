<style>
    .fullHeightForm {
        height: 100%;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
        align-items: center;
        padding: 15px;
    }

    .mud-input {
        height: 36px;
    }

</style>

@if (ProductFamily != null)
{
    <EditForm Model="@ProductFamily" class="fullHeightForm" OnSubmit="Save">
        <DataAnnotationsValidator />
        <ValidationSummary />
        <MudGrid>
            <MudItem xs="3">
                <MudTextField Disabled=@(!ProductFamily.IsEditing) @bind-Value=@ProductFamily.Name For="@(()=>ProductFamily.Name)" Margin="Margin.Dense" Variant="MudBlazor.Variant.Outlined" />
            </MudItem>
            <MudItem xs="7">
                <MudTextField Disabled=@(!ProductFamily.IsEditing) @bind-Value=@ProductFamily.Comment For="@(()=>ProductFamily.Comment)" Margin="Margin.Dense" Variant="MudBlazor.Variant.Outlined" />
            </MudItem>
            <MudItem xs="1">
                <MudButton ButtonType="MudBlazor.ButtonType.Submit" Disabled="@(!ProductFamily.IsEditing)" Variant="MudBlazor.Variant.Filled">Valider</MudButton>
            </MudItem>
            <MudItem xs="1">
                <MudButton Disabled="@(!ProductFamily.IsEditing)" OnClick="Cancel" Variant="MudBlazor.Variant.Filled">Annuler</MudButton>
            </MudItem>
        </MudGrid>
    </EditForm>
}
